# AICONTEXT — рабочая память проекта (для AI-ассистента)

Этот файл — память для работы над проектом. Читать в начале каждой сессии.
Дополняй его, когда узнаёшь что-то новое и неочевидное.

## Что это за проект

Тулинг для блога рецептов **ourgoodrecipes.blogspot.com**. Один скрипт
`blogger_format.mjs` (Node, ESM) тянет посты через **Blogger API v3**, парсит
сырой текст рецепта в структуру (вступление / ингредиенты / шаги / советы) и
перезаписывает пост красивым HTML — две карточки: 🥗 Ингредиенты и
👩‍🍳 Приготовление. Оригинал сохраняется ниже под пунктирной линией как
черновик (маркер `— оригинальный текст (черновик) —`), чтобы повторные прогоны
не оборачивали уже оформленный HTML (`getOriginal()` достаёт чистый оригинал).

## Файлы

| Файл | Назначение |
|------|-----------|
| `blogger_format.mjs` | Основной скрипт-конвертер + парсер |
| `manual_overrides.json` | Ручные списки ингредиентов/шагов для «грязных» постов (по id) |
| `blogger_token.json` | OAuth-токены Blogger API — **в git нет** (gitignore) |
| `posts_backup.json` | Бэкап оригиналов постов — **в git нет**, генерируется из API |
| `preview_<id>.html` | Превью при `--dry --id` — **в git нет** |

## API / токены

- Blog ID: `4333353184444059143`
- Проект Google Cloud: **Lingua** (`lingua-490422`), Blogger API v3, scope `auth/blogger`
- Аккаунт блога: `andrei1pazniak@gmail.com`
- **OAuth consent в режиме Testing → refresh-токен живёт ~7 дней.** Когда
  протухнет — переавторизоваться и обновить `blogger_token.json` (инструкция
  в `README.md`). Симптом: `No access token` при запуске.
- Чтение: `GET /blogger/v3/blogs/{BLOG_ID}/posts` (постранично, `fetchBodies=true`).
- Запись: `PUT .../posts/{postId}` с `{id, title, content, labels}`. Между записями пауза 700 мс.
- ⚠️ **PUT заменяет пост целиком** — `labels` ОБЯЗАТЕЛЬНО слать обратно, иначе метки
  стираются. `updatePost` берёт `post.labels` из живого поста и шлёт их назад.
- **15 меток блога:** Вторые блюда (39), Салаты (15), рыба (9), Десерт (8),
  выпечка (7), Суп (7), Вегетерианская кухня (5), скороварка (4), Закуски (4),
  Индия (2), Детское (2), Соусы (2), варенье (1), вок (1), Закатки (1).

## Команды

```bash
node blogger_format.mjs            # оформить только НОВЫЕ (неоформленные) посты
node blogger_format.mjs --all      # переоформить все (оригинал из черновика)
node blogger_format.mjs --id=ID    # один пост
node blogger_format.mjs --ids=A,B  # конкретные посты
node blogger_format.mjs --dry      # предпросмотр без записи
node blogger_format.mjs --dry --id=ID   # + сохранить preview_<id>.html
node blogger_format.mjs --backup   # из кэша posts_backup.json, не из API (не тратит токен)
```

`--dry --all --backup` — безопасный способ прогнать парсер по всем постам без
обращения к API и без записи (удобно для диагностики).

## Парсер: 3 уровня (по приоритету), см. `formatPost()`

1. **Manual override** — если id есть в `manual_overrides.json`, берём готовые
   `intro/ingredients/steps/notes` оттуда. Override заменяет ВСЁ (если задал
   только ingredients — steps станут пустыми, всегда задавай и то и другое).
2. **Structured** (`parseStructuredHTML`) — посты с заголовками h2/h3, под-рецепты,
   под-группы («Для теста»), под-шаги, блоки «Важно/Совет».
3. **Plain** (`parseContent`) — без разметки: сначала `<ul>`=ингредиенты +
   `<ol>`=шаги, иначе эвристика (строка с количеством = ингредиент, предложение
   с глаголом-инструкцией = шаг, пустая строка = граница).

`blogger_format.mjs` экспортирует `formatPost, parseContent, parseStructuredHTML,
getOriginal, OVERRIDES` и запускает `main()` только при прямом вызове — так что
функции можно импортировать в диагностические скрипты.

## Защита от порчи данных

- `posts_backup.json` — бэкап оригиналов до оформления.
- `getOriginal()` — на повторном прогоне достаёт чистый оригинал из черновика.
- `isAlreadyFormatted()` — обычный прогон не трогает уже оформленные посты.
- `DONE_IDS` (в скрипте) — посты, оформленные вручную; авто-прогон (`--all` тоже)
  их пропускает. Достижимы только точечно через `--id`/`--ids`.
- ⚠️ **Гача:** у hand-styled постов из `DONE_IDS` «оригинал» — это уже готовая
  карточка (а не сырой текст) и без маркера черновика. Прогон `--id`/`--ids` по
  ним кладёт старую карточку под черту → рецепт дублируется. Для таких постов
  оригинал держать не нужно (он = дубль): пушим только чистую карточку без
  хвоста-черновика. Так уже сделано для `7750670184181847377` (салат №2).

## Договорённости по контенту (важно для качества)

- **Количества должны быть прописаны прямо в шагах**, а не только в списке
  ингредиентов — чтобы при готовке не приходилось возвращаться к списку
  («добавьте 130 г муки», а не «добавьте муку»). Это требование владельца блога.
- Прозаические рецепты (ингредиенты вшиты в текст шагов) и посты со списком без
  количеств парсятся плохо → выносим их в `manual_overrides.json` вручную.
- Для под-рецептов с несколькими соусами в плоском списке ингредиентов
  использую префиксы («Соус: …», «Бешамель: …»).
- **Оригинал пока держим в конце статьи** (под пунктирной линией, как делает
  форматтер по умолчанию). Это временно — пока владелец не убедится, что
  ингредиенты/шаги перенесены правильно. ⏳ TODO: когда проверит — удалить блок
  оригинала из постов (всё после маркера `— оригинальный текст (черновик) —`),
  оставив только оформленную карточку.

## История работы

- **2026-06-06:** Добавлены ручные override'ы для 16 постов: 11 с пустым/неполным
  списком ингредиентов (прозаические рецепты — черничный торт, рыба в тамаринде,
  лазанья, мачанка, апельсиновые блинчики и др.) + 5, где количества были только
  в списке, а не в шагах (лосось со шпинатом, салаты из свёклы, холодник, куриное
  филе в сухарях). Аудит количеств в шагах → 0 проблемных. В скрипт добавлены
  экспорты + guard на `main()` для диагностики. **Запушено в блог вживую**
  (`--all` + точечно `--ids=7750670184181847377` для салата из свёклы №2,
  который в `DONE_IDS`) — 99 постов обновлено, 0 ошибок. Оригинал каждого поста
  пока сохранён в конце под чертой — удалить после проверки переноса (см. TODO выше).
- **2026-06-06 (QA-проход):** Прогнал автоаудит по всем 100 постам (скрипт
  проверял: потерянные числа, шаги в списке ингредиентов, пустые шаги, мусор,
  один гигантский шаг). Нашёл и починил:
  - **Баг парсера:** регулярка срезания буллетов ела цифру, приклеенную к дефису
    (`-15 г` → `г`). Исправлено (коммит). Задет был 1 пост («Тост с авокадо»).
  - **17 рецептов** с реальным mis-parse (шаги склеились в список ингредиентов,
    салаты без шагов, прозаические рецепты, англоязычный «Палтус», мусор от
    расширения Ginger в «Брокколи») → вынесены в `manual_overrides.json` с
    количествами в шагах. Все запушены вживую.
  - В `_qa.mjs`-аудите остались только ложные срабатывания (длинные, но корректные
    строки ингредиентов). Для будущих QA-проходов удобно временно воссоздать такой
    аудит: импортирует парсер, сравнивает распарсенные ing/steps с эвристиками.
- **2026-06-06 (оценка верности + починка):** Прогнал грейдер (5 параллельных
  агентов) — каждый рецепт оценён 1–10 по соответствию оригиналу. Средняя вышла
  8.1. Вскрылся системный дефект: у plain-рецептов ингредиенты **без чисел**
  утекают в шаги (салаты-слои, выпечка с секциями тесто/начинка/заливка), а у
  structured — соус/начинка падает в intro/notes. Починил 20 худших (оценка 3–6)
  ручными override'ами с количествами в шагах → пере-оценка 8–10, средняя по
  блогу поднялась до ~9.2. Всего в `manual_overrides.json` теперь 56 записей.
  Корневую эвристику парсера не трогал (риск регресса 60 хороших).
- **Способ оценить заново:** временный скрипт строит JSON `{original, rendered}`
  по каждому посту (impорт `getOriginal`/`OVERRIDES`/парсера), агенты-грейдеры
  читают и ставят баллы по рубрике (10 — всё перенесено; 4–6 — потеря ингредиентов/
  шагов; 1–3 — мусор). notes — законная карточка для советов/вариантов.
- **2026-06-06 (косметика 7–8):** Добавлен фильтр `isJunkLine()` в парсер — выкидывает
  из ингредиентов/шагов/intro мусорные строки: голые URL, атрибуции («Оригинал…тут»,
  «Рецепт взят с …»), остатки расширения Ginger. Работает в `blockToLines`, `liText`
  (list-парсер) и в intro structured/list-парсеров. ⚠️ JS `\b` НЕ работает после
  кириллицы — детектор не полагается на `\b`. Скан по всем 100 показал ровно 11
  мусорных строк в 7 постах (все реально мусор) — они зачищены и запушены вживую.
- **2026-06-06 (полировка #1–6):** Обзорный проход → правки:
  - **#1** Пост `735536498470936607` был без title (название только в теле) —
    проставлен «Миндальный Лимонный Кекс».
  - **#2** Удалён дубль-пост «Перец фаршированный рисом» `7387084043284003071`
    (через Blogger API DELETE), его override убран. В блоге теперь **99 постов**.
  - **#3** В `isJunkLine` добавлен срез одиноких заголовков-обрывков
    (`Готовка:/Готовим!/Работа:`), которые секционный парсер не потребляет.
    ⚠️ НЕ добавлять туда слова мод-переключения (Приготовление/Состав/Ингредиенты).
  - **#5** Дубль «Салат из печени трески» (`8173…`/`6368…`) — это две РАЗНЫЕ версии
    (с граммовками vs. грубая), оставлены обе намеренно.
  - **#6** Корневой фикс: `ING_INLINE` расширен на `потребуется|понадоб|припас` —
    списки без чисел под таким заголовком теперь заходят в ингредиенты, а не в шаги.
    Плюс `parseContent` теперь принимает title и отсекает строку-заголовок из intro
    (`isTitleLine`: только точное совпадение или фрагмент, НЕ «title + лишний текст»).
  - Картинки: 66 из 99 постов без фото — главный визуальный пробел (нужны реальные
    фото, авто-сгенерировать нельзя).
